node_modules/
.idea/workspace.xml
.idea/*.iml

# Vercel CLI project links (created by `vercel link`)
.vercel

# Local secrets. .env.example files are documentation and stay committed.
*.env
.env
.env.local
!.env.example

# Uncompressed photo/video masters. Kept locally as the source for the
# optimised WebP/MP4 under website/, but too heavy to carry in the repo —
# re-export from these rather than upscaling what the site serves.
assets-source/
