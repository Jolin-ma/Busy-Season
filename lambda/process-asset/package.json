{
  "name": "legacylink-process-asset",
  "version": "1.0.0",
  "description": "Lambda: compress uploads to WebP, extract video keyframes, run Rekognition moderation",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip dist/ node_modules/"
  },
  "dependencies": {
    "@aws-sdk/client-rekognition": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "fluent-ffmpeg": "^2.1.3",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.140",
    "@types/fluent-ffmpeg": "^2.1.27",
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0"
  }
}
